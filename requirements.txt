# REST-only стек (браузерная автоматизация исключена)
python-dotenv>=1.0      # чтение .env (секреты вебхука)
PyYAML>=6.0             # config.yaml
openpyxl>=3.1           # выгрузка в Excel (часть 1)
# pandas>=2.2           # опционально, для группировки (часть 1) — включить при реализации
# REST-клиент Bitrix24 — из установленного скилла bitrix24-agent (stdlib, без зависимостей)
