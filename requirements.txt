# REST-only стек (браузерная автоматизация исключена)
python-dotenv>=1.0      # чтение .env (секреты вебхука)
PyYAML>=6.0             # config.yaml
openpyxl>=3.1           # выгрузка в Excel (Фаза 2 «Парсинг»: листы день/группировка/метаданные)
# REST-клиент Bitrix24 — из установленного скилла bitrix24-agent (stdlib, без зависимостей)

# GUI (Фаза 6): десктопная обёртка над export/fill
PySide6>=6.6.0         # Qt6-биндинги; GUI-режим main.py (запуск без аргументов)

# Тесты (без сети):
pytest>=8.0
