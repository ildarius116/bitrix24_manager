# Скопируй в .env и заполни. .env в git не попадает (см. .gitignore).
# Код вебхука — СЕКРЕТ. После завершения работ перегенерируй вебхук на портале.

B24_DOMAIN=your-portal.bitrix24.ru
B24_AUTH_MODE=webhook
B24_WEBHOOK_USER_ID=1
B24_WEBHOOK_CODE=

# Опционально — аудит REST-вызовов:
# B24_AUDIT_FILE=.runtime/b24_audit.jsonl
