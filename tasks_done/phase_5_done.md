# Отчёт о завершении Фазы 5 — Надёжность и эксплуатация

**Дата закрытия:** 2026-06-26
**Исходные спецификации:** `tasks/phase_5_00_overview.md`, `phase_5_01_idempotency.md`,
`phase_5_02_dry_run.md`, `phase_5_03_logging_audit.md`, `phase_5_04_tests.md`
**Статус:** DONE

---

## Что сделано по подзадачам

### 5_01 — Идемпотентность (журнал обработанных дней)

Новый модуль `src/journal.py` — локальный журнал `.runtime/processed.json` с TTL:
- `load_journal` / `is_processed` / `mark_processed` — чтение, проверка и запись с очисткой
  просроченных записей по TTL.
- Атомарная запись: сначала во временный файл `processed.json.tmp`, затем `os.replace`; при
  исключении `.tmp` очищается — журнал никогда не остаётся в повреждённом состоянии.
- Новый ключ `config.yaml`: `runtime.journal_ttl_days: 7`; геттер `Config.journal_ttl_sec`
  с защитой от нечислового значения.
- **Интеграция в `run_fill`:** в боевом режиме день, уже отмеченный в журнале в пределах TTL,
  получает статус `skipped` без обращения к порталу; после успешной боевой записи (`filled`)
  вызывается `mark_processed` (обёрнут в `try/except` — потеря журнала не роняет пайплайн).
- В `--dry-run` журнал не читается и не пишется.
- Журнал — дополнительный слой поверх главного гарда `_reread_guard` (перепроверка 4-дневного
  окна и пустоты перед каждой реальной записью).

### 5_02 — dry-run / no-interaction

Подтверждено: флаги `--dry-run` / `--no-interaction` / `--confirm-write`, режим plan-only и
сводная таблица статусов были полностью реализованы в Фазах 1 и 4. Новых правок в коде не
потребовалось; задача закрыта подтверждением.

### 5_03 — Логирование и аудит REST

`src/b24.py` расширен единой аудируемой точкой вокруг `client.call`:
- Аудит охватывает `item_list_all` и `batch`.
- Путь к файлу: геттер `get_audit_file_path(config)` → `config.env.audit_file` →
  `B24_AUDIT_FILE` из `.env` → дефолт `.runtime/bitrix24_audit.jsonl`.
- В каждую строку аудита пишутся: `method`, `param_keys` (только **имена** ключей, без
  значений), `status`, `duration_ms`, `tenant` (domain). Значения параметров и код вебхука
  в аудит не попадают.
- Запись в аудит-файл обёрнута в `try/except` — ошибка журналирования не роняет REST-вызов.
- Структурный лог и маскирование секрета вебхука в выводе реализованы ещё в Фазе 1 и
  сохранены.
- Follow-up (не блокер): аудит включён по умолчанию и растёт без ротации — при накоплении
  больших объёмов рекомендуется настроить ротацию `.runtime/bitrix24_audit.jsonl` (например,
  `logrotate` или периодическая очистка вручную).

### 5_04 — Тесты

Добавлены три новых тест-файла и расширен существующий:

| Файл | Что покрывает |
|------|---------------|
| `tests/test_journal.py` | TTL-границы (ровно на TTL — ещё живой; TTL+1с — просрочен), битый JSON-файл, атомарность записи, отсутствие секретов в журнале |
| `tests/test_audit.py` | Формат JSONL-строки; отсутствие значений параметров и кода вебхука в аудите; `try/except` не роняет вызов при недоступном пути |
| `tests/test_export_excel.py` | `build_workbook`: 3 листа («Данные», «Группировка», «Метаданные»); агрегаты по группировке; рамки дат; согласованность итоговых часов |
| `tests/test_fill.py` (дополнение) | `TestRunFillJournal`: skip в боевом режиме для записи в журнале, вызов `mark_processed` после `filled`, dry-run не трогает журнал |

Итог тестов: **218 passed** (124 до фазы + 94 новых). Сеть не используется.

---

## Созданные и изменённые файлы

| Файл | Действие | Примечание |
|------|----------|-----------|
| `src/journal.py` | создан | load_journal, is_processed, mark_processed, атомарная запись, TTL-очистка |
| `src/b24.py` | изменён | аудит REST (get_audit_file_path, JSONL-запись вокруг client.call, batch) |
| `config.yaml` | изменён | новый ключ `runtime.journal_ttl_days: 7` |
| `src/config.py` | изменён | геттер `Config.journal_ttl_sec` (защита от нечислового значения) |
| `src/fill.py` | изменён | интеграция журнала в run_fill (skip по журналу, mark_processed после filled, обёртка try/except) |
| `tests/test_journal.py` | создан | юнит-тесты журнала |
| `tests/test_audit.py` | создан | юнит-тесты аудита |
| `tests/test_export_excel.py` | создан | юнит-тесты build_workbook |
| `tests/test_fill.py` | изменён | добавлен класс TestRunFillJournal |

---

## DoD-статус

| Критерий | Статус |
|----------|--------|
| 5_01: повторный запуск не создаёт дублей (журнал TTL + `_reread_guard`) | ВЫПОЛНЕН |
| 5_02: `--dry-run` и `--no-interaction` реализованы (Фазы 1/4), подтверждены | ВЫПОЛНЕН |
| 5_03: структурный лог + аудит REST (JSONL, без секретов/значений) | ВЫПОЛНЕН |
| 5_04: ключевая логика покрыта тестами на синтетике, без сети | ВЫПОЛНЕН |
| Фаза 5 (DoD): повторный запуск не создаёт дублей; есть --dry-run/--no-interaction; ведётся структурный лог + аудит REST; ключевая логика покрыта тестами | ВЫПОЛНЕН |

---

## Проверки (факт)

- `venv/Scripts/python.exe -m pytest -q` → **218 passed**. Сеть не используется.
- **Код-ревью (reviewer):** блокеров не было. Применены все Should-fix и Minor:
  - обёртка `mark_processed` в `run_fill` в `try/except`;
  - очистка `.tmp` при сбое атомарной записи;
  - защита геттера `journal_ttl_sec` от нечислового значения;
  - ужесточён тест аудита (проверка отсутствия значений, не только ключей).
- Остался follow-up (не блокер): рост `.runtime/bitrix24_audit.jsonl` без ротации —
  рекомендуется документировать и при необходимости добавить ротацию.

---

## Безопасность

- Аудит REST пишет только имена ключей (`param_keys`), не значения; код вебхука в файл
  аудита не попадает.
- Журнал `.runtime/processed.json` содержит только ID элементов и временные метки — без
  секретов.
- Оба файла лежат в `.runtime/` (в `.gitignore`).

> Напоминание (CLAUDE.md §4): после полного завершения работ по проекту — перегенерировать
> вебхук на портале (`/devops/` → «ПЕРЕГЕНЕРИРОВАТЬ»).
