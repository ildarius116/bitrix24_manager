"""GUI-обёртка (PySide6) над модулями src/ проекта bitrix24 «Рабочий день».

GUI — только презентационный слой: вся бизнес-логика остаётся в `src/`,
CLI (`main.py`) продолжает работать параллельно. Точка входа — `gui_main.py`
в корне проекта.
"""
