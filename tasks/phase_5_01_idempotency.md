# phase_5_01 — Идемпотентность записи

**Статус:** TODO
**Фаза:** 5 (Надёжность)
**Зависит от:** phase_4_03
**NFR:** 2

## Цель
Исключить повторное создание учёта при двойном запуске.

## Объём работ
- Непосредственно перед `crm.item.add` перечитать день и убедиться, что `ufCrm46_1742997115` всё ещё пуст; иначе — пропуск со статусом «уже заполнено».
- Локальный журнал обработанных дней (`.runtime/processed.json`: id дня, дата, время, id созданного 1218) с TTL.
- Повторная проверка 4-дневного окна перед записью.

## Критерии приёмки (DoD)
- Второй запуск подряд не создаёт дубль (день определяется как заполненный/обработанный).
- Журнал корректно пополняется и читается.

## Артефакты
`src/fill.py` + `.runtime/processed.json` (gitignore).
