# phase_4_01 — Исследование механизма кнопки «Выполнено»

**Статус:** DONE (вывод исправлен 2026-06-26; см. tasks_done/phase_4_05_done_button_fix.md)
**Фаза:** 4 (Создание учёта)
**Зависит от:** phase_2_01, phase_2_02
**FR:** 2.1.7
**Режим:** ТОЛЬКО ЧТЕНИЕ (без записи в прод)

## Цель
Понять, что технически делает кнопка «Выполнено» в окне «Заполнить работы по договорам»:
смена стадии дня (1208), флаг, запуск бизнес-процесса — или это просто UI-обёртка над
созданием дочернего 1218 (Bitrix сам привязывает родителя через `parentId1208`).

## Объём работ (наблюдение)
- Сравнить «пустой» и «заполненный» дни 1208 по полям стадии/флагов: `ufCrm46Flag`, `categoryId`, `previousStageId`, `movedTime`, и связь `ufCrm46_1742997115`.
- Проверить связь: у существующих учётов 1218 `parentId1208` указывает на день, а у дня в `ufCrm46_1742997115` — ID учёта (двусторонняя привязка). Установить, кто её создаёт (форма/БП/авто).
- При необходимости — посмотреть `bizproc.workflow.instances`/`template.list` для 1208 (см. references/available-methods.md), есть ли БП на «Выполнено».
- Зафиксировать вывод: достаточно ли одного `crm.item.add` 1218, или нужен доп. шаг.

## Критерии приёмки (DoD)
- В этом файле (или PRD §2.6) записан однозначный вывод о механизме «Выполнено».
- Определён план записи для phase_4_03 (только add vs add + смена стадии/БП).

## Артефакты
Запись вывода здесь + при необходимости read-only пробы в `out/`.

---

## ВЫВОД (read-only дискавери, 2026-06-25) — DONE

> **⚠ УСТАРЕЛО — прежний вывод об отсутствии доп. шага ОПРОВЕРГНУТ боевым прогоном 2026-06-26.
> Истинный механизм «Выполнено» — см. раздел «ИСПРАВЛЕНИЕ ВЫВОДА» ниже.**

**Однозначный вывод о механизме «Выполнено».** Кнопка «Выполнено» в окне «Заполнить работы
по договорам» — это UI-обёртка над созданием дочернего «Учёта рабочего времени» (1218) с
привязкой к дню через `parentId1208`. **Связь двусторонняя и создаётся ядром Bitrix
автоматически: достаточно одного `crm.item.add` 1218 с `parentId1208` = ID дня** — портал сам
добавляет новый ID в `1208.ufCrm46_1742997115` и пересчитывает агрегаты дня.

**Доказательства (read-only):**
- Сравнение заполненного дня id=270608 (25.06.2026) и пустого id=254912 (01.06.2026): оба
  `categoryId=61`, у обоих `stageId` ОТСУТСТВУЕТ (у 1208 нет воронки/стадий), `ufCrm46Flag='Y'`
  одинаков (НЕ маркер заполненности). При заполнении меняются только авто-агрегаты дня:
  `ufCrm46_1742997115` (`[163025]` vs `[]`), `ufCrm46_1742341767` («По договорам, ч» 8 vs 0),
  `ufCrm46_1751352025` ('8:00' vs '0:00'), `ufCrm46_1745314005` (связанный агрегат). Всё это
  система пересчитывает сама.
- Двусторонняя связь подтверждена: учёт 1218 id=163025 имеет `parentId1208=270608`, день 270608
  имеет `ufCrm46_1742997115=[163025]`. Создаётся ядром при add (не формой/не отдельным БП).

**Обязательные поля 1218 (`crm.item.fields`):** `ufCrm48_1742996936` (Договор, type=crm,
required), `ufCrm48_1744239302` (Описание задачи, string, required), `opened` (boolean, required,
но есть дефолт). Все required перекрываются планируемым payload — доп. шага «add недостаточно»
НЕ требуется. `ufCrm48_1754894889="2"` — технический строковый ID договора, парный к коду
`T512_2` (для «Общих задач подразделения»). `categoryId` у 1218 необязателен (дефолт=63), но
воронок две — **63 «Работы/задачи по договорам»** (isDefault) и 65 «Отгулы»; задавать `categoryId:63`
ЯВНО, чтобы гарантированно не попасть в «Отгулы». Стадий у 1218 нет — `stageId` задавать нечего.

**План записи для phase_4_03 — один `crm.item.add`, без доп. шагов:**
```
crm.item.add  entityTypeId=1218  fields:
  parentId1208:        <ID дня 1208>     # привязка; обратную связь в день Bitrix пропишет сам
  categoryId:          63                # явно — воронка «Работы/задачи по договорам»
  ufCrm48_1742996936:  "T512_2"          # Договор (required), дефолт «Общие задачи подразделения»
  ufCrm48_1754894889:  "2"               # [тех] ID договора, парный к T512_2
  ufCrm48_1744239302:  "<описание>"      # Описание задачи (required), дефолт «Общие задачи подразделения»
  ufCrm48_1742996959:  <часы>            # Количество часов (дефолт 8)
```
НЕ нужно: писать `ufCrm46_1742997115` у дня вручную (создастся авто — иначе риск дублей), менять
стадию (стадий нет), запускать БП вручную.

**Открытые вопросы (выяснить write-тестом, с явного разрешения):**
- `ufCrm48_1743029170` («Итог», числовой дубликат часов): неясно, проставляет ли его система сама
  из часов или это делает форма. На первом боевом add НЕ передавать; перечитать — если не
  проставился, добавить в payload.
  **ЗАКРЫТО (боевой тест 2026-06-26, учёт id=163736):** «Итог» проставлен системой = 8 (равен
  часам), хотя в payload не передавался. Также автогенерируется `title` = «Общие задачи
  подразделения | 8:00». Вывод: `build_payload` корректен, «Итог» в payload добавлять НЕ нужно.
- БП/роботы на 1208 через REST проверить нельзя — у вебхука нет scope `bizproc`
  (`bizproc.*` → insufficient_scope). На план записи не влияет (любой БП триггерится сам после add).

**Безопасность:** выполнялись только read-only вызовы (`crm.item.get`, `crm.item.list`,
`crm.item.fields`, `crm.category.list`; `bizproc.*` отклонены по scope). Ни одного add/update/delete.
Код вебхука не печатался. Временный скрипт удалён.

---

## ИСПРАВЛЕНИЕ ВЫВОДА (2026-06-26)

**Прежний вывод опровергнут** боевым прогоном 2026-06-26: после создания учёта 1218 (id=163736) для
дня id=271557 этот день **продолжал числиться в фильтре «Запланированные»** портала.

### Истинный механизм «Выполнено» (доказано read-only дискавери)

День 1208 уходит из «Запланированные» не через поля самого дня (стадий нет, `ufCrm46Flag` не меняется),
а через **завершение CRM-дела типа To-Do**, привязанного к карточке дня:

- Дела: `PROVIDER_ID="CRM_TODO"`, `OWNER_TYPE_ID=1208`, `OWNER_ID=<id дня>`.
- Завершить дело = `crm.activity.update` с `fields: {COMPLETED: "Y"}` (scope `crm`).
- **Создание учёта 1218 и закрытие CRM-дела — два независимых действия.**

**Доказательная база (read-only):**
- День id=271557 (26.06): учёт [163736] УЖЕ есть, но дело id=169987 имело `COMPLETED="N"` → день
  висел в «Запланированные».
- День id=267932 (22.06, уже «выполнен»): дело `COMPLETED="Y"`.
- Старый день id=254912 (01.06, вне окна): CRM-дел нет (дела по истечении срока могут удаляться или
  не создаваться — на поведение нашего алгоритма не влияет).

### Итоговый план записи для FR-2.1.7 (два шага)

1. `crm.item.add entityTypeId=1218 fields:{parentId1208, ...}` — создать учёт (как было).
2. `crm.activity.update id=<id дела> fields:{COMPLETED:"Y"}` — завершить CRM-дело дня.
   Дело находится через `crm.activity.list filter:{OWNER_TYPE_ID:1208, OWNER_ID:<id дня>, PROVIDER_ID:"CRM_TODO"}`.

Шаг 2 выполняется **только после успешной верификации шага 1** (день должен оставаться видимым
«Запланированным» сигналом проблемы, если учёт не прошёл верификацию).

Провайдер и параметры поиска дела берутся из `config.yaml` (`activity_provider_id: "CRM_TODO"`).

### Реализация (внесена в код)

- `config.yaml`: `activity_provider_id: "CRM_TODO"`.
- `src/config.py`: атрибут `Config.activity_provider_id`.
- `src/b24.py`: методы `B24.activity_list(...)` и `B24.activity_complete(...)` (plan→execute, dry-run-safe).
- `src/workday.py`: `select_repair_days(...)` — дни в окне 4 дней с непустым `works` (учёт есть, но
  дело могло остаться открытым).
- `src/fill.py`: `_window_guard`, `complete_day_activities`, `verify_activities_closed`; в `run_fill` —
  после создания учёта (только если верификация прошла) закрываем дело; добавлен «ремонт»-проход по
  `select_repair_days`; ранний return при отсутствии кандидатов убран. Дело не закрывается при сбое
  верификации учёта.
- `main.py`: тексты fill обновлены; `has_errors` учитывает `activity_ok is False`.
- Тесты: 261 passed (новые тесты: `complete_day_activities` / `verify_activities_closed` /
  `select_repair_days` / «ремонт»-проход в `run_fill` / verify-fail-no-close).

### Боевой прогон (2026-06-26, с разрешения пользователя)

Закрыто дело id=169987 на дне id=271557 (status "repaired", верификация ok, exit 0).
Прочие 4 дня в окне — already-closed. Новых учётов не создавалось.

**Статус:** DONE (доработка оформлена в tasks/phase_4_05_done_button_fix.md).
