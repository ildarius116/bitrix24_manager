# phase_4_03 — Создание элемента «Учёт рабочего времени» (1218)

**Статус:** TODO
**Фаза:** 4 (Создание учёта)
**Зависит от:** phase_4_01, phase_4_02
**FR:** 2.1.8–2.1.10.4
**Режим:** запись в прод — только по явному разрешению

## Цель
Создать дочерний элемент 1218 под отобранным днём через `crm.item.add` (двухфазно).

## Объём работ
- Сформировать `fields`:
  ```json
  {
    "parentId1208": <ID дня>,
    "ufCrm48_1742996936": "<contract_general_tasks из config = T512_2>",
    "ufCrm48_1754894889": "2",
    "ufCrm48_1744239302": "<описание>",
    "ufCrm48_1742996959": <часы>
  }
  ```
- Двухфазно: `--plan-only` → показать payload → подтверждение → `--execute-plan --confirm-write`.
- Учесть вывод phase_4_01 (если нужен доп. шаг смены стадии/БП — добавить).
- Повторная проверка 4-дневного окна и пустоты непосредственно перед записью (см. phase_5_01).
- `--dry-run`: показать payload, ничего не писать.

## Критерии приёмки (DoD)
- В `--dry-run` печатается корректный payload без записи.
- В боевом режиме (с разрешения) элемент создаётся, ответ содержит ID нового 1218.

## Артефакты
`src/fill.py` (запись).
