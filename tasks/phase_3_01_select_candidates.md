# phase_3_01 — Отбор кандидатов на заполнение

**Статус:** TODO
**Фаза:** 3 (мелкая — одной подфазой)
**Зависит от:** phase_2_01
**FR:** 2.1.1–2.1.5

## Цель
Из последних записей «Рабочий день» отобрать те, которым нужно создать учёт: в пределах
4-дневного окна и с пустым столбцом «Работы/задачи за день».

## Объём работ (функционально законченная задача)
- `crm.item.list` 1208 `order:{id:desc}` → **верхние 5** (FR-2.1.1).
- Дата из `ufCrm46_1742342657` (FR-2.1.2; запасной парсер из title).
- **4-дневное окно (FR-2.1.3):** оставить `дата >= сегодня − edit_window_days` (календарные, включительно; «сегодня» — Europe/Moscow).
- **Пустота (FR-2.1.4/5):** оставить дни, где `ufCrm46_1742997115` пуст (`[]`/None).
- Остальные — лог «пропуск + причина» (старше окна / уже заполнено / нет даты).
- Функция `select_candidates(days, cfg, today) -> list[WorkdayDay]`.

## Критерии приёмки (DoD)
- Юнит-тест на синтетике: верно отсекаются «вне окна», «заполнено», «нет даты».
- На реальных данных список кандидатов логируется с причинами пропусков.

## Артефакты
`src/workday.py` (`select_candidates`).
