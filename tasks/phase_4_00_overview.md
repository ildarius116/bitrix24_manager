# Фаза 4 — Создание учёта рабочего времени (FR-2.1.6–2.1.10.4)

**Статус:** TODO
**Зависит от:** Фаза 3
**Связано:** PRD §2.6, FR-2; PLAN «Этап 4».

## Цель фазы
Для отобранных дней создать дочерний элемент «Учёт рабочего времени» (1218) с привязкой к дню,
договором «Общие задачи подразделения», описанием и часами (дефолт + подтверждение).

## Результат фазы (DoD)
На одном дне (после явного разрешения на запись) создаётся учёт; привязка подтверждается
перечитыванием (`ufCrm46_1742997115` дня содержит новый ID). `--dry-run` показывает payload без записи.

## Подзадачи
1. `phase_4_01_done_button_investigation.md` — выяснить механизм «Выполнено» (read-only).
2. `phase_4_02_values_prompt.md` — дефолт + подтверждение значений.
3. `phase_4_03_create_log.md` — `crm.item.add` 1218 (plan→execute).
4. `phase_4_04_verify.md` — верификация результата.

## Важно
- По умолчанию режим «только чтение»: боевые записи в прод — лишь по явному разрешению заказчика.
- Строго соблюдать 4-дневное окно (проверка повторно перед записью).

---

## СТАТУС (2026-06-25) — КОД ГОТОВ, боевой тест отложен

**Сделано и проверено:**
- **4_01** (механизм «Выполнено») — DONE. Вывод: достаточно одного `crm.item.add` 1218 с
  `parentId1208`; Bitrix сам создаёт двустороннюю связь и пересчитывает агрегаты дня. Стадий у
  1208 нет, доп. шагов/БП со стороны пайплайна не нужно. План записи зафиксирован в
  `phase_4_01_done_button_investigation.md`.
- **4_02/4_03/4_04** — код реализован в `src/fill.py` (`collect_values`, `build_payload`,
  `_reread_guard`, `create_log`, `verify_log`, `run_fill`) + проводка `_cmd_fill` в `main.py` с
  гейтом боевой записи `--confirm-write`. Новые ключи в `config.yaml`
  (`entity.timelog_category_id=63`, `fields.log_contract_tech`, `defaults.contract_tech_id="2"`)
  и геттеры в `src/config.py`.
- **Тесты:** `tests/test_fill.py` — 81 юнит-тест (без сети); всего по проекту **124 passed**.
- **Ревью:** блокеров нет; write-путь безопасен. Применены предзапусковые правки: пересчёт
  `today` в гарде (защита от полуночи), ранняя проверка fill-конфига, упрощение защёлки.
- **`--dry-run` против портала (read-only):** пайплайн отрабатывает end-to-end, ничего не пишет.

**Гарантии безопасности (заложены в код):**
- Боевая запись возможна ТОЛЬКО при `--confirm-write` И БЕЗ `--dry-run`; по умолчанию — dry-run.
- Перед каждой записью — гард: перечитать день, заново проверить 4-дневное окно (по свежей дате
  Europe/Moscow) и пустоту «Работ за день» (идемпотентность, fail-closed).
- Код вебхука не печатается/не логируется.

**ОТЛОЖЕНО (DoD боевой части):**
- Боевой тест на одном дне (`crm.item.add` 1218 + верификация привязки) НЕ выполнен: в текущем
  4-дневном окне (21–25.06.2026) нет ни одного незаполненного дня — кандидатов 0. Решение
  заказчика (2026-06-25): **ждать появления подходящего дня** в окне, затем выполнить боевой
  тест с отдельным явным разрешением.

**ОТКРЫТЫЙ ВОПРОС к закрытию во время боевого теста:**
- Поле «Итог» `ufCrm48_1743029170` сейчас в payload НЕ передаётся (гипотеза: система проставит
  его из часов сама). На первом боевом add перечитать учёт и проверить; если «Итог» не
  проставился — добавить его в `build_payload`.
