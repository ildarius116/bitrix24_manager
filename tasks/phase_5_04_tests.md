# phase_5_04 — Тесты (без сети)

**Статус:** TODO
**Фаза:** 5 (Надёжность)
**Зависит от:** Фазы 2–4

## Цель
Покрыть ключевую логику юнит-тестами на синтетических данных (без обращения к порталу).

## Объём работ
- `tests/`:
  - даты: `extract_date`, `parse_cli_date`, `within_edit_window` (граничные: сегодня−4 и сегодня−5).
  - отбор кандидатов: вне окна / заполнено / нет даты / валидный.
  - формирование payload 1218 (правильные поля/значения).
  - Excel-экспорт: 3 листа, агрегаты группировки, рамки дат.
- Мок REST-обёртки (`b24`) фикстурами.

## Критерии приёмки (DoD)
- `pytest` зелёный без доступа к сети.
- Граничные случаи окна 4 дня проверены явно.

## Артефакты
`tests/*`.
